import 'package:flutter/material.dart';

class SelectAiPersonality extends StatelessWidget {
  const SelectAiPersonality({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('3'),
    );
  }
}
